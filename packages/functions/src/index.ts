// @closet/functions — Edge handlers + business logic. Consumed by per-handler
// subpath imports from the Deno shims (supabase/functions/<name>/index.ts), so this
// barrel is intentionally empty. Handlers land under src/<domain>/.
export {};
