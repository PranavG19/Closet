// @closet/mobile — the Expo app (built in the FRONTEND phase, docs/04). Feature
// domains land under features/<root> (roots declared in conventions.json). Colors
// come from useTokens() only. Barrel starts empty so the scaffold typechecks before
// Expo/React deps are installed. This file carries no react import for that reason.
export {};
