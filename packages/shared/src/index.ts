// @closet/shared — the type SSOT. Zod schemas, pure functions, and port interfaces
// live here. Populated by the backend tasks; this barrel starts empty so the
// scaffold typechecks. Add exports as schemas/ports land (each re-exported here).
export {};
